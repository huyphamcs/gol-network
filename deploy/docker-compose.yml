name: gol

services:
  postgres:
    image: postgres:17.6-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: gol
      POSTGRES_USER: gol_admin
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD:-}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks: [private]
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U gol_admin -d gol']
      interval: 5s
      timeout: 5s
      retries: 20

  migrate:
    image: postgres:17.6-alpine
    environment:
      PGPASSWORD: ${POSTGRES_ADMIN_PASSWORD:-}
      POSTGRES_RUNTIME_PASSWORD: ${POSTGRES_RUNTIME_PASSWORD:-}
    volumes:
      - ../agent/src/db/schema.sql:/migrations/001.sql:ro
      - ./migrate.sh:/migrations/migrate.sh:ro
    networks: [private]
    depends_on:
      postgres: { condition: service_healthy }
    command: ['sh', '/migrations/migrate.sh']
    restart: 'no'

  web:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: web
    restart: unless-stopped
    env_file:
      - path: .env.production
        required: false
    environment:
      DATABASE_URL: postgresql://gol_runtime:${POSTGRES_RUNTIME_PASSWORD:-}@postgres:5432/gol
      GOL_SOURCE_COMMIT: ${RELEASE_COMMIT:-}
      LANGGRAPH_AGENT_URL: http://langgraph-agent:8124/agent
      GOL_AGENT_SHARED_SECRET: ${GOL_AGENT_SHARED_SECRET:-}
      # Credential boundary: the web container can obtain no AWS credentials at all, so it can
      # never call kms:Sign or kms:GetPublicKey. Independently testable with `docker compose exec`.
      AWS_EC2_METADATA_DISABLED: 'true'
    # Caddy is the only public ingress; no host port is published for the application itself.
    networks: [public, private]
    depends_on:
      migrate: { condition: service_completed_successfully }
      langgraph-agent: { condition: service_healthy }
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:3000/app/api/health').then(r=>{if(!r.ok)process.exit(1)})",
        ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    security_opt: ['no-new-privileges:true']

  langgraph-agent:
    build:
      context: ../langgraph-agent
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      AAVE_MCP_URL: ${AAVE_MCP_URL:-https://mcp.aave.com/mcp}
      AAVE_MCP_DISCOVERY: ${AAVE_MCP_DISCOVERY:-true}
      ARC_RPC_URL: ${ARC_RPC_URL:-https://rpc.testnet.arc.network}
      ARC_AAVE_POOL_ADDRESS: ${ARC_AAVE_POOL_ADDRESS:-}
      GOL_AGENT_SHARED_SECRET: ${GOL_AGENT_SHARED_SECRET:-}
      GOL_LANGGRAPH_MODEL: ${GOL_LANGGRAPH_MODEL:-gpt-5.5-2026-04-23}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    # The service has outbound model/MCP access but no database, wallet, Privy, or AWS signer
    # credentials. Next.js reaches it only over the private network and proxies AG-UI streams.
    networks: [public, private]
    healthcheck:
      test:
        [
          'CMD',
          'python',
          '-c',
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8124/health', timeout=3)",
        ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 10s
    security_opt: ['no-new-privileges:true']

  worker:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: worker
    restart: unless-stopped
    env_file:
      - path: .env.production
        required: false
    environment:
      DATABASE_URL: postgresql://gol_runtime:${POSTGRES_RUNTIME_PASSWORD:-}@postgres:5432/gol
      GOL_SOURCE_COMMIT: ${RELEASE_COMMIT:-}
      # The worker is the only process that talks to AWS KMS. Credentials come from the EC2
      # instance role over IMDS (hop limit 2 required); no static AWS keys enter the container.
      AWS_REGION: ${AWS_KMS_SIGNER_REGION:-ap-northeast-1}
    # The worker needs outbound access to Privy, Arc, OpenAI, and AWS KMS as well as private
    # PostgreSQL. It publishes no port, so `public` grants egress without any inbound exposure.
    networks: [public, private]
    depends_on:
      migrate: { condition: service_completed_successfully }
    stop_grace_period: 45s
    security_opt: ['no-new-privileges:true']

  caddy:
    image: caddy:2.10.2-alpine
    restart: unless-stopped
    environment:
      DOMAIN: ${DOMAIN:-}
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks: [public]
    depends_on:
      web: { condition: service_healthy }

networks:
  public:
  private:
    internal: true

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
